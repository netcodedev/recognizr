# Google Photos API Configuration
# Copy this file to .env and fill in your actual values
# Note: These are prefixed with PUBLIC_ because they are used in client-side code
# In a production environment, consider moving the OAuth flow to the server side

# Google OAuth Client ID (from Google Cloud Console)
PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id-here

# Google OAuth Client Secret (from Google Cloud Console)
PUBLIC_GOOGLE_CLIENT_SECRET=your-google-client-secret-here
